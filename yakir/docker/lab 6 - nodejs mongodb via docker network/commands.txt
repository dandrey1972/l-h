docker network ls

docker network create app-net

docker run -d --network app-net  --name mongodb -p 27020:27017 -e MONGO_INITDB_ROOT_USERNAME=root -e MONGO_INITDB_ROOT_PASSWORD=secret mongo:latest

docker inspect mongodb

docker network inspect app-net

docker run --network app-net --name app -d -p 3000:3000 lab5:0.0.1   

docker logs -f app