# Homework
- Run the app without dockerfile
- py.exe app.py
- Browse vie google chrome
  
```
http://127.0.0.1:5000/
Run the app with dockerfile
```
 ## Dockerfile 
 - Wrap the app via Dockerfile
 - Create an images
 - Run the image ( Docker run )
 - Tag and push the image into your docker hub account
